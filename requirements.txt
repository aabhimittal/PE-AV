# Core runtime dependencies for PE-AV.
# For a CPU-only install of PyTorch use:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
numpy>=1.23
pyyaml>=6.0

# Dev / test (optional):
#   pip install -e ".[dev]"
